mixin chart(spec)
  // Minimal placeholder for charts; in production you could hydrate with client JS
  pre(class="mt-4 bg-gray-50 rounded-2xl p-4 text-xs text-gray-700")= JSON.stringify(spec, null, 2)


