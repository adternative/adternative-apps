extends frame

block pageContent
  - const moduleName = (typeof appKey === 'string' ? appKey.toUpperCase() : 'APP');
  div(class="max-w-3xl mx-auto")
    div(class="bg-white shadow rounded-xl p-8 border border-gray-200")
      h1(class="text-2xl font-semibold text-gray-900 mb-4") Access to #{moduleName} is restricted
      p(class="text-gray-600 mb-6")
        | This area is available only to users who have an active subscription to the
        |  #{moduleName} app#{user && user.role === 'admin' ? ' (admins always have full access).' : '.'}
      if subscribeUrl
        a(href=subscribeUrl target="_blank" rel="noopener noreferrer" class="inline-flex items-center justify-center px-5 py-3 rounded-lg font-medium transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-teal-500 text-white hover:bg-purple-900 focus:ring-purple-500")
          | Go to subscription page
      else
        a(href=`https://adternative.ro/app/${appKey || ''}` target="_blank" rel="noopener noreferrer" class="inline-flex items-center justify-center px-5 py-3 rounded-lg font-medium transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-teal-500 text-white hover:bg-purple-900 focus:ring-purple-500")
          | Go to subscription page


