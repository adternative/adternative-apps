doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= "Error"
    link(rel='stylesheet', href='/stylesheets/dist.css')
  body(class="h-screen bg-purple-950 flex items-center justify-center")
    div(class="rounded-2xl p-8 w-1/2 lg:w-full")
      h1(class="text-3xl font-bold text-white mb-4") #{(error && error.status) || (typeof status !== 'undefined' ? status : 500)}: #{message || 'An unexpected error occurred'}
      if error.stack
        pre(class="bg-black p-4 rounded-3xl overflow-auto text-sm text-teal-400")= error.stack
      div(class="mt-6")
        a(href="/" class="text-teal-400 hover:text-teal-300 inline-flex items-center justify-center px-4 py-2 rounded-lg font-medium transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-purple-600 text-white hover:bg-purple-700 focus:ring-purple-500")
          i(data-lucide="move-left" class="w-4 h-4 mr-2")
          | Return to Home

    script(src="https://unpkg.com/lucide@latest/dist/umd/lucide.js")
    script.
      lucide.createIcons();
